# سجلّ التغييرات

يُولَّد آلياً من أوصاف الالتزامات. لا يُحرَّر يدوياً.

## 3.3.0 — 2026-07-29

### ميزات

- قبولُ مُصدِرٍ سابق مدّةَ نقل المركز إلى نطاق مخصّص (`59474e0`)
- ربط الدخول بالمتصفّح الذي بدأه — إغلاق تثبيت الجلسة (`64d49c2`)

### إصلاحات

- سقفٌ على إعادة جلب JWKS، وREADME يصف ما شُحن بعد v2.0.0 (`b24dc96`)
- اسم مفتاح الجلسة تجزئةُ المعرّف لا المعرّف — قراءةُ KV لم تعد تعطي كوكيّات (`a3a442d`)
- التحقق يفرّق «الرمز باطل» من «عجزتُ عن الفحص»، ومهلةٌ على كل نداء للمركز (`d19ad0a`)

## 3.2.0 — 2026-07-28

### ميزات

- إشعار خروج خلفي — الخروج من المركز يُنهي جلسات المنصات (`b470b39`)

## 3.1.2 — 2026-07-28

### إصلاحات

- مطالبةٌ غائبة لا تُكتب فوق قيمة قائمة (`880b560`)
- صفٌّ قائم يُحدَّث ولا يُدرَج — عمودٌ إلزامي لا تعرفه الحزمة كان يُسقط كل دخول (`b36de96`)

## 3.1.1 — 2026-07-28

### إصلاحات

- الدخول يبلّغ المركز بالصلاحية، فلا يبقى العمود فارغاً أبداً (`d71bacd`)

## 3.1.0 — 2026-07-28

### ميزات

- خروجٌ يظهر أثره، ووجهةُ عودةٍ لا تكون مساراً برمجياً (`95dbb10`)

### إصلاحات

- مسار الاستقبال ليس وجهةَ عودة — دورةٌ لا تنتهي كانت تُقطع بخطأ المتصفّح (`73ae68c`)

## 3.0.0 — 2026-07-28

### تغييرات كاسرة

- شكل الردّ يتبع طبيعة الطلب، والوسيط يعيد محتوى الرمز (`edb125e`)

## 2.0.0 — 2026-07-28

### تغييرات كاسرة

- مطابقة عقد naf-id الفعلي والتحقق في كل طلب محمي (`5560872`)

## 1.0.0 — 2026-07-27

### تغييرات كاسرة

- حزمة naf-auth — جسر المصادقة مع naf-id (#1) (`edd5af8`)
- تثبيت العقد العام للحزمة عند v1.0.0 (`86106a4`)

### ميزات

- خطّاف onClaims وتعريفات الأنواع (`250c3eb`)
- بناء حزمة naf-auth — الوسيط ومسار الاستقبال والتحقق (`b7de219`)

### إصلاحات

- تعطيل عمودَي perms و last_seen_at من wrangler.toml (`6a52c2b`)
- توحيد صورة المُصدِر قبل مطابقة iss (`4ec91c7`)

يُشتقّ المحتوى من عناوين الالتزامات منذ آخر وسم، ويُنشئه سير عمل الإصدار عند الدمج في `main`.

ولم يُقطع إصدار بعد.
