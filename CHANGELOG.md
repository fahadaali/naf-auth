# سجلّ التغييرات

يُولَّد آلياً من أوصاف الالتزامات. لا يُحرَّر يدوياً.

## 1.0.0 — 2026-07-27

### تغييرات كاسرة

- حزمة naf-auth — جسر المصادقة مع naf-id (#1) (`edd5af8`)
- تثبيت العقد العام للحزمة عند v1.0.0 (`86106a4`)

### ميزات

- خطّاف onClaims وتعريفات الأنواع (`250c3eb`)
- بناء حزمة naf-auth — الوسيط ومسار الاستقبال والتحقق (`b7de219`)

### إصلاحات

- تعطيل عمودَي perms و last_seen_at من wrangler.toml (`6a52c2b`)
- توحيد صورة المُصدِر قبل مطابقة iss (`4ec91c7`)

يُشتقّ المحتوى من عناوين الالتزامات منذ آخر وسم، ويُنشئه سير عمل الإصدار عند الدمج في `main`.

ولم يُقطع إصدار بعد.
